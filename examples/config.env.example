# Example configuration for /etc/retro-ha/config.env

# Repo pinning for the one-time installer bootstrap (required)
RETRO_HA_REPO_URL=https://github.com/theaussiepom/retro-ha-appliance.git
RETRO_HA_REPO_REF=main

HA_URL=
NFS_SERVER=
NFS_PATH=

# ROM sync (Pattern A)
# ROMs are copied from NFS on boot to a local directory.
# Gameplay does not depend on NFS being available.
#
# Optional:
# RETRO_HA_NFS_MOUNT_POINT=/mnt/retro-ha-roms
# RETRO_HA_NFS_MOUNT_OPTIONS=ro
# RETRO_HA_NFS_ROMS_SUBDIR=roms
# RETRO_HA_ROMS_DIR=/var/lib/retro-ha/retropie/roms
# RETRO_HA_ROMS_SYNC_DELETE=0
# RETRO_HA_ROMS_OWNER=retropi:retropi
# RETRO_HA_ROMS_SYSTEMS=nes,snes,megadrive,psx
# RETRO_HA_ROMS_EXCLUDE_SYSTEMS=ps2,ps3,switch

# Optional display rotation
# RETRO_HA_SCREEN_ROTATION=normal

# Controller button codes (optional)
#
# This project uses Linux evdev button codes. Different controllers can emit
# different codes, so you can configure them here.
#
# To discover codes on the Pi:
#   sudo retro-ha-controller-codes.sh
#   # then press the buttons you want.
#
# Enter Retro (HA -> Retro):
# RETRO_HA_RETRO_ENTER_TRIGGER_CODE=315
#
# Exit Retro (Retro -> HA): press RETRO_HA_RETRO_EXIT_SECOND_CODE, then press
# RETRO_HA_RETRO_EXIT_TRIGGER_CODE within RETRO_HA_COMBO_WINDOW_SEC.
# RETRO_HA_RETRO_EXIT_TRIGGER_CODE=315
# RETRO_HA_RETRO_EXIT_SECOND_CODE=304
#
# Timing knobs (optional):
# RETRO_HA_START_DEBOUNCE_SEC=1.0
# RETRO_HA_COMBO_WINDOW_SEC=0.75

# Local save/state directories (always local)
# RETRO_HA_SAVES_DIR=/var/lib/retro-ha/retropie/saves
# RETRO_HA_STATES_DIR=/var/lib/retro-ha/retropie/states

# Optional: periodic backup of saves/states to NFS (skips while in Retro mode)
# RETRO_HA_SAVE_BACKUP_ENABLED=0
# RETRO_HA_SAVE_BACKUP_DIR=/mnt/retro-ha-backup
# RETRO_HA_SAVE_BACKUP_SUBDIR=retro-ha-saves
# RETRO_HA_SAVE_BACKUP_DELETE=0
# RETRO_HA_SAVE_BACKUP_NFS_SERVER=
# RETRO_HA_SAVE_BACKUP_NFS_PATH=
# RETRO_HA_SAVE_BACKUP_NFS_MOUNT_OPTIONS=rw

# Optional: install RetroPie during first boot (heavy; may take a long time)
# RETRO_HA_INSTALL_RETROPIE=0

# LED MQTT control (optional)
RETRO_HA_LED_MQTT_ENABLED=0
MQTT_HOST=
MQTT_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_TLS=0
RETRO_HA_MQTT_TOPIC_PREFIX=retro-ha
